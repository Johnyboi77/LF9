# SO wenig Abhängigkeiten wie möglich! 

# List all in python installed dependencies:
pip list 

# Check of MariaDB in DBeaver
systemctl status mariadb

# im Terminal -- Einloggen (mit dem .env/Local_PW
sudo mariadb -u root -p